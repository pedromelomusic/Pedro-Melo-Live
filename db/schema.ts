import {sqliteTable,text,integer,index,primaryKey,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const requests=sqliteTable('requests',{id:text('id').primaryKey(),song:text('song').notNull(),songId:text('song_id'),sessionId:text('session_id'),name:text('name').notNull(),created:integer('created').notNull(),status:text('status').notNull().default('pending'),client:text('client').notNull()},t=>[index('requests_created').on(t.created),index('requests_client_created').on(t.client,t.created),index('requests_session_created_id').on(t.sessionId,t.created,t.id)]);
export const settings=sqliteTable('settings',{key:text('key').primaryKey(),value:text('value').notNull()});
export const songs=sqliteTable('songs',{id:text('id').primaryKey(),title:text('title').notNull(),artist:text('artist').notNull(),status:text('status').notNull().default('available'),lyrics:text('lyrics').notNull().default(''),lyricsApproved:integer('lyrics_approved').notNull().default(0),revision:integer('revision').notNull().default(0)},t=>[index('songs_status_title').on(t.status,t.title)]);
export const sessions=sqliteTable('sessions',{id:text('id').primaryKey(),name:text('name').notNull(),date:text('date').notNull().default(''),mode:text('mode').notNull().default('concert'),created:integer('created').notNull(),requestsOpen:integer('requests_open').notNull().default(1),nowSongId:text('now_song_id'),archived:integer('archived').notNull().default(0),revision:integer('revision').notNull().default(0)});
export const sessionSongs=sqliteTable('session_songs',{sessionId:text('session_id').notNull(),songId:text('song_id').notNull(),status:text('status').notNull().default('available'),inSetlist:integer('in_setlist').notNull().default(0),position:integer('position').notNull().default(0)},t=>[primaryKey({columns:[t.sessionId,t.songId]}),index('session_songs_order').on(t.sessionId,t.inSetlist,t.position)]);
export const metrics=sqliteTable('metrics',{day:text('day').notNull(),sessionId:text('session_id').notNull(),event:text('event').notNull(),count:integer('count').notNull().default(0)},t=>[primaryKey({columns:[t.day,t.sessionId,t.event]})]);
export const subscriptions=sqliteTable('subscriptions',{id:text('id').primaryKey(),channel:text('channel').notNull(),contact:text('contact').notNull(),sessionId:text('session_id'),created:integer('created').notNull(),expires:integer('expires').notNull(),policy:text('policy').notNull(),tokenHash:text('token_hash').notNull()},t=>[index('subscriptions_expires').on(t.expires),index('subscriptions_token').on(t.tokenHash)]);
export const outbox=sqliteTable('outbox',{id:text('id').primaryKey(),destination:text('destination').notNull(),event:text('event').notNull(),payload:text('payload').notNull(),subscriptionId:text('subscription_id'),created:integer('created').notNull(),attempts:integer('attempts').notNull().default(0),nextAttempt:integer('next_attempt').notNull().default(0),lease:integer('lease').notNull().default(0)},t=>[index('outbox_due').on(t.nextAttempt,t.lease),index('outbox_created').on(t.created)]);
export const rateBuckets=sqliteTable('rate_buckets',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()},t=>[index('rate_buckets_expires').on(t.expires)]);
export const exportJobs=sqliteTable('export_jobs',{id:text('id').primaryKey(),scope:text('scope').notNull(),snapshot:integer('snapshot').notNull(),total:integer('total').notNull(),upperRow:integer('upper_row').notNull(),cursor:integer('cursor').notNull().default(0),count:integer('count').notNull().default(0),state:text('state').notNull().default('pending'),uploadId:text('upload_id').notNull(),parts:text('parts').notNull().default('[]'),bufferKey:text('buffer_key').notNull().default(''),lease:integer('lease').notNull().default(0),expires:integer('expires').notNull(),error:text('error').notNull().default('')},t=>[index('export_jobs_expires').on(t.expires)]);
export const tips=sqliteTable('tips',{id:text('id').primaryKey(),sessionId:text('session_id'),requestId:text('request_id'),songId:text('song_id'),provider:text('provider').notNull(),expectedCents:integer('expected_cents').notNull(),receivedCents:integer('received_cents').notNull().default(0),refundedCents:integer('refunded_cents').notNull().default(0),paidVotes:integer('paid_votes').notNull().default(0),state:text('state').notNull().default('pending'),externalRef:text('external_ref'),created:integer('created').notNull(),revision:integer('revision').notNull().default(0)},t=>[uniqueIndex('tips_provider_reference').on(t.provider,t.externalRef),index('tips_session_created').on(t.sessionId,t.created)]);
export const databaseRevision=sqliteTable('database_revision',{id:integer('id').primaryKey(),revision:integer('revision').notNull().default(0)});
export const restoreJobs=sqliteTable('restore_jobs',{id:text('id').primaryKey(),baseRevision:integer('base_revision').notNull(),sourceKey:text('source_key').notNull(),safetyKey:text('safety_key').notNull(),hash:text('hash').notNull(),summary:text('summary').notNull(),expires:integer('expires').notNull(),applied:integer('applied').notNull().default(0),nonce:text('nonce')});
export const contactChallenges=sqliteTable('contact_challenges',{id:text('id').primaryKey(),subscriptionId:text('subscription_id').notNull(),tokenHash:text('token_hash').notNull(),expires:integer('expires').notNull(),confirmed:integer('confirmed').notNull().default(0)},t=>[index('contact_challenges_token').on(t.tokenHash),index('contact_challenges_expires').on(t.expires)]);
