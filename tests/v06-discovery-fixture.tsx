// Browser QA entrypoint only; generated bundle is ignored and never shipped.
import {createRoot} from 'react-dom/client';
import Home from '../app/page';
createRoot(document.getElementById('root')!).render(<Home/>);
