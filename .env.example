ADMIN_EMAIL=
# Optional integrations. Keep empty until the provider workflow is configured.
MAKE_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=
INTEGRATIONS_ENABLED=false
# Comma-separated channels enabled only after Make is configured: email,whatsapp,instagram
COMMUNICATION_CHANNELS=
# At least 32 random characters; bearer secret for scheduled POST /api/maintenance
MAINTENANCE_SECRET=
